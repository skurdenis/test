query {
  repository(owner: $owner!, name: $name!) {
    milestones(last: 100) {
      nodes {
        issues(last: 100) {
          nodes {
            closedAt
            closed
          }
        }
        state
        dueOn
        title
      }
    }
  }
}
